//! Sums of tick data.

use std::ops::Add;

pub struct DataSums {
    // todo
}

impl Add for DataSums {
    type Output = DataSums;

    fn add(self, rhs: Self) -> Self::Output {
        DataSums {
            
        }
    }
}